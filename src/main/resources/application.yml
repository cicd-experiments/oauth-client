server:
  forward-headers-strategy: native
spring:
  application:
    name: oauth-client
  profiles:
    active: develop
  security:
    oauth2:
      client:
        registration:
          ${OAUTH_REGISTRATION_ID}:
            provider: keycloak
            authorization-grant-type: authorization_code
            client-id: ${OAUTH_CLIENT_ID}
            client-name: ${OAUTH_CLIENT_NAME}
            client-secret: ${OAUTH_CLIENT_SECRET}
            client-authentication-method: client_secret_basic
            redirect-uri: ${OAUTH_HOSTNAME}/login/oauth2/code/${OAUTH_REGISTRATION_ID}
            scope:
              - openid
        provider:
          keycloak:
            issuer-uri: ${IAM_TOOL_HOSTNAME}/realms/${IAM_REALM_NAME}
